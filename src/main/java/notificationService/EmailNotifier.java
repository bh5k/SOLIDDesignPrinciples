package notificationService;

public class EmailNotifier implements Notifier {

    @Override
    public void notify(Notification notification) {
        /* actually do something with the notification */
    }
}
